# API service deps (layered on shared/requirements-base.txt)
fastapi==0.121.1
uvicorn==0.32.1
pydantic==2.12.5  # floor set by google-genai 2.x; do not drop below
boto3==1.35.76

# WSI deep-zoom tiling. openslide-bin ships the native OpenSlide library as a wheel,
# so no apt/system packages are needed (see docs/docker_credential_issue.md sibling
# note on this machine's no-apt Dockerfile convention).
Pillow==11.0.0
openslide-python==1.4.1
openslide-bin

# Published spatial-annotation import (etl/jobs/spatial_manifest.py, til_overlay_import.py).
# pydicom reads the published DICOM Segmentation objects; pyarrow reads the IDC index
# parquet files used to match our slides against public annotation collections.
pydicom==3.0.1
pyarrow==18.1.0

# Source-link analysis for links with no connector (shared/analysis.py). Uses the
# Gemini free tier: Google Search grounding reads the dataset page, and a response
# schema forces the answer into the same report shape the connector path returns.
# 2.x is required: the Interactions API rejects the 1.x request schema outright
# ("legacy Interactions API schema is no longer supported", May 2026 breaking change).
google-genai==2.12.1
