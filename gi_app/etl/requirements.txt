# ETL-specific deps (layered on shared/requirements-base.txt)
openpyxl==3.1.5        # read Data.xlsx (used by connectors/gdc_acquire.py)
boto3==1.35.76         # S3/MinIO client (storage.py)
# Reserved for later ETL stages (kept here so the image is ready):
duckdb==1.1.3
pyarrow==18.1.0
